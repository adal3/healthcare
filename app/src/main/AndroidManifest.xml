<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthcare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HealthArticlesDetailsActivity"></activity>
        <activity android:name=".HealthArticlesActivity" />
        <activity android:name=".BuyMedicineBookActivity" />
        <activity android:name=".CartBuyMedicineActivity" />
        <activity android:name=".BuyMedicineDetailActivity" />
        <activity android:name=".BuyMedicineActivity" />
        <activity android:name=".OrderDetailsActivity" />
        <activity android:name=".LabTestBookActivity" />
        <activity android:name=".CartLabActivity" />
        <activity android:name=".LabTestDetailsActivity" />
        <activity android:name=".LabTestActivity" />
        <activity android:name=".BookAppointmentActivity" />
        <activity android:name=".DoctorDetailActivity" />
        <activity android:name=".FindDoctorActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>